# 数据互通规划

## 原则

两个项目先独立完善，互通能力作为后续阶段逐步加入。第一阶段优先使用 JSON 导入/导出，避免过早抽取共享代码。

## 可互通数据

- 项目基础信息：项目名称、SKU、产品信息、交付日期
- 任务节点：节点名称、所属项目大类、开始日期、结束日期、状态
- 资源信息：执行人、角色、产能占用
- 日历规则：休息日、假期、展示周期

## 建议阶段

1. 定义统一 JSON 字段。
2. 两个项目分别支持导出自身数据。
3. 其中一个项目支持导入另一个项目导出的 JSON。
4. 稳定后再考虑抽取共享解析工具。
