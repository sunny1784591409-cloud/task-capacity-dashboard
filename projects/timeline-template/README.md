# 在线项目排期模板

这是独立维护的在线排期模板项目。

## 使用方式

直接打开 `index.html`，或从根目录项目选择页进入。

## 文件说明

- `index.html`：页面结构
- `styles.css`：页面样式
- `script.js`：排期、模板、自定义大类、休息日、假期、导出等逻辑

## 数据保存

模板配置保存在浏览器本地缓存中。不同浏览器、不同设备之间不会自动同步。
